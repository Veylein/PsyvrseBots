FROM python:3.11-slim
WORKDIR /app

COPY requirements.txt /app/
RUN python -m pip install --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

COPY . /app

EXPOSE 5000
# Run the bot entrypoint which starts the bot and the internal API server
CMD ["python", "-m", "villicus.main"]
